# 🗄️ Archived root orientation — Hot-update blocks 2026-06-06 → 2026-07-22

_Archived from `CLAUDE.md` on 2026-08-23. These blocks were all still marked "Hot update" in the root
orientation file, which meant nine blocks each implicitly claiming to be current state — the exact failure
mode vault-canon rule 4 exists to prevent._

**These are HISTORICAL. Do not quote as current state.** Source-of-truth order for "what is true now" is
unchanged: (1) tail of `wiki/log.md`, (2) `wiki/results/*.md`, (3) `wiki/research-candidates.md`,
(4) `wiki/index.md`, (5) root `CLAUDE.md`.

The durable *framing rules* that were embedded in these blocks — the ones that constrain how results may be
restated — were promoted into the HARD RULES section of `CLAUDE.md` rather than archived with them. The
narrative below is provenance; the rules are live.

**Correction recorded at archive time (2026-08-23):** the 2026-07-22 block's "Open / not done" list for the
BENCH lane was verified against the repo and is **\~6/8 stale**. `UNIVERSALITY.json` was regenerated
(2026-07-23), `commit-confluence/README.md` was updated the same day, `GATE_FAILURES.json` was deleted rather
than left stale, and `.exit` attestation files exist including `universality_regen.exit` and
`resume_reattest_2026-07-22.exit`. Only the two hash-binding items — `extension_manifest_sha256` and a
`matrix_sha256` binding, both genuinely absent from `SUMMARY.json` — remain open.

---

## Hot update for Claude (2026-07-22, BENCH strict Phase-4 CLOSED — Family-A split: A1 PASS 10/10, A2 FAIL 6/10 = intrinsic SIGN-FLIP; B1 = gate cascade)

The registered **BENCH strict Phase-4** confirmatory run (seed 20260711, nboot 2000, 10 models × 6 tasks, launched 2026-07-15) **finished clean 2026-07-22**. Detail: [[results/bench-a2-signflip-2026-07-22]]; log tail 2026-07-22. **Sealed 18/20 byte-unperturbed** (Phase 4 wrote only under `profiles_bench/`); no torch/mlx-vlm pooling.

**Headline — the frontier moved: a registered TRANSFER verdict.** **A1 PASS 10/10** (bar ≥8; halueval_qa per-model cluster-geometric deployability; all ten OK, controls pass, n=1000/500 stems, weakest CI-lo 0.6705). **A2 FAIL 6/10** (bar ≥8, `aborted=false`; fixed cell `fusion_rank_mean_geom` + ONE sign fit on the 9-model pool, applied blind LOMO) ⇒ **the pre-registered A1∧A2 conjunction is NOT satisfied** — "the floor extends to HaluEval-QA" is licensed **only** in per-model-calibration form, never as a transferable fixed detector.

**A2's failure is an intrinsic SIGN-FLIP, not signal absence.** The four misses sit far *below* 0.5 — Mistral-7B **0.174**, Mistral-Nemo **0.206**, Qwen2.5-7B **0.276**, Phi-3.5 **0.394** (confidently backwards). Each independently selects `+1` in its own calibration while all six passers select `−1`; reversing rescues them (0.826/0.794/0.724/0.606). **Reversal is NOT a rescue for A2** — knowing to reverse requires the holdout's labels, which is exactly what A1 may use and A2 may not. **Verified from raw matrices** (production `append_fusion_columns` re-imported; reconstruction reproduces registered AUROCs to 3 dp): mean fused rank faithful/hallucinated mirrors exactly — Llama-3.2-3B 0.62/0.38 (high=faithful) vs Mistral-7B 0.37/0.63 (high=hallucinated).

**Polarity is GENERATION-SPLIT, not a family law.** Mistral (both +1) and Llama (both −1) are family-coherent, but **Qwen and Phi split by generation** (Qwen2.5 flips / Qwen3 holds; Phi-3.5 flips / Phi-4 holds) — independently corroborated by [[results/delta-sigma-onaxis-2026-05-15]], which already found Phi-3.5 (−) vs Phi-4 (+) on a different diagnostic. Descriptive only (1–2 reps per subgroup; family confounded with tokenizer/architecture/size).

**Framing rule — restate carefully.** This establishes **no universal *orientation*** (and, with 8 distinct A1 winners across 10 models, no universal *best* cell) — but **NOT** the absence of a common informative cell: `fusion_rank_mean_geom` with a per-model sign clears >0.55 on **all ten**. A2 rejects the compound **"fixed cell + fixed sign"** deployment, not cell identity. Do not restate this as "no informative cell exists."

**B1 replication reads 7/20 FAIL — a pre-registered GATE CASCADE, not a geometric collapse. Do NOT propagate it as a signal negative.** `_endpoint_value` (`run_bench.py:784`) zeroes every cell of any task with ≥3 COMMITMENT-FAIL cells (§4 zero error budget × §8.1 systematic abort), so all 10 `triviaqa_paired_rep` cells — **including 7 terminal-status-OK** — are forced False. Layers: raw geometry **18/18 deployable** (CI-lo 0.6577–0.9804) → pre-cascade **14/20** → post-§8.1 **7/20**. Triggers are rare (Llama-3.1-8B 1/1000, Qwen3-1.7B 1/1000, gemma-3-4b 12/1000 — gemma sometimes answers the trivia question instead of judging faithfulness). The anli_r1_rep behavioral fails (Phi-3.5 `'\n'`, Qwen2.5-7B `' To'` CoT) are the signatures Amendment A1 explicitly declined to rescue — pre-disclosed. Per §8 amendment discipline the rule **cannot** be retroactively softened without a new registration.

**[RESOLVED — screened NULL 2026-07-25]** the three-model flip cluster (Mistral-7B, Mistral-Nemo, Qwen2.5-7B) is *exactly* the v4 sealed **E_A2 partial-transfer** trio (`research-candidates` §5) — but the frozen designed-retrospective screen (`stage_b/SIGNFLIP_COINCIDENCE_DESIGN.md` @ `1f70c9f`, run verbatim 2026-07-25) found **no association** (Fisher two-sided p=0.50; positive-orientation majority on the aux tasks is the cohort norm 7/9; non-trio Phi-4 sign-identical to both Mistrals). Coincidence at this resolution; no follow-up motivated; cannot rule out a sub-family. Detail: `wiki/results/signflip-coincidence-2026-07-25.md`.

**Verification:** three independent read-only reviews concur — fresh-eyes (fable), Codex static audit (re-derived endpoints from matrices, not SUMMARY.json), and Codex `gpt-5.6-sol` high-reasoning sign-structure read (confounds cleared: sign application consistent, identical label polarity across all NPZs, no per-model fusion polarity, commit-parse gates validity only).

**Open / not done:** registered A2 **lifecycle exit unattested** (`strict_phase4_a4.exit` never written — re-run the registered resume and capture the genuine status; **do not manufacture the file**); descriptive `UNIVERSALITY.json` not regenerated; `SUMMARY.json` omits `extension_manifest_sha256`; no `matrix_sha256` binding (future schema); `GATE_FAILURES.json` **stale** (Jul 14, pre-A3 — regenerate or delete); `commit-confluence/README.md` materially stale; paper carries no BENCH/HaluEval result and its label-cost paragraph still omits the paired-stem (row-vs-stem) disclosure. An **A5-style amendment** (per-cell commitment error budget + widened acceptable-answer template, blip-vs-behavior split) is *proposed, not filed* — it must be pre-registered blind for a future re-run, never applied retroactively.

## Hot update for Claude (2026-06-25, Qwen2.5-32B stress panel 8/8 + Furnace operator guard shipped; 06-23 closures)

**06-30 operational update — local Qwen2.5-7B chat TUI is wired.** `/Users/msrk/Documents/furnace-guard` now defaults to the cached `mlx-community/Qwen2.5-7B-Instruct-4bit`. `./furnace tui` is a persistent MLX chat session rather than a score-only console: first-run setup discovers cached models/profiles, remembers the model and Monitor/Strict policy, measures before generation, displays the Furnace state plus explicitly limited next-token confidence, and only then streams the response. Monitor suppresses `BLOCK`/`DEFER` and asks before continuing on `ABSTAIN`; Strict only speaks on calibrated `ALLOW`. `/model`, `/mode`, `/clear`, and `/calibrate PATH` are live; calibration accepts local labeled JSONL and only marks a fixed-cell profile deployable at n>=150, >=25/class, and bootstrap CI_lo>0.5. Real `Hi` smoke passed through the exact `./furnace tui` entrypoint: ANLI-domain mismatch was shown as `ABSTAIN`, user confirmation was requested, then Qwen replied. The model stays warm after the initial \~90-second load. The existing ANLI profile is **not** a general harmful-prompt safety classifier; automatic chat safety blocking needs a dedicated domain calibration.

**06-26 operational cleanup — Furnace guard repo de-clouded.** `/Users/msrk/Documents/furnace-guard` no longer carries the Modal runtime files (`modal_app.py`, `_commit_dump.py`) or Modal CLI path. The repo README now states the current guard direction: run locally on the **Mac mini M4**, keep model/profile artifacts local, and remote in with **RustDesk** when a desktop is useful. `furnace` / `furnace_cli.py` call a local guard command and fail closed to `DEFER` when scoring is unavailable. Historical Modal/torch results and docs remain provenance for the exploratory scale/stress panel; they are not the live guard repo architecture.

**Qwen2.5-32B nf4 stress panel — 8/8 deployable** (Modal torch, NON-byte-comparable, **does NOT alter the sealed 18/20**). Added ANLI R2/R3, TruthfulQA-MC, and HaluEval QA/dialogue/summarization on top of the existing 32B `anli_r1`/`triviaqa_paired` cells; all six new tasks n=200, 0 drops, 100% YES/NO, controls pass, o_proj cos=1.0. Geom CI-lo: anli_r1 0.763 / anli_r2 0.744 / anli_r3 0.698 / triviaqa 0.781 / truthfulqa_mc 0.730 / halueval_qa 0.809 / halueval_dialogue 0.539 / halueval_summarization 0.553. **Key finding — the locus broadens: the "Qwen family → attention-morphology" shorthand (stated flatly in the 2026-06-22 block below) must now be SCOPED to the ANLI/TriviaQA/TruthfulQA panel.** ANLI R1/R2/R3 + TruthfulQA stay on penultimate-layer ACE attention at t=0; but HaluEval **QA → Fusion** and **dialogue/summarization → readout/surprise (gen_step=1)** ⇒ broader grounded-dialogue / source-faithfulness prompts can pull Qwen into the commit/readout region too. Exploratory, not a registered benchmark expansion (standard row bootstrap on grouped/stem-paired tasks; HaluEval contexts char-limited for practical attention capture). Detail: `wiki/results/qwen32b-stress-2026-06-25.md`; log tail 2026-06-25.

**Shipped — operator-facing Furnace guard (Qwen2.5-32B).** `guard_prompt()` in `/Users/msrk/Documents/furnace-guard/modal_app.py` loads the fitted profile + score matrix, sign-locks the endpoint winner, derives a conservative Youden threshold, and returns `ALLOW`/`BLOCK`/`ABSTAIN`/`DEFER` **without emitting response text** (fail-closed → `DEFER` on missing/non-deployable profile, control fail, unsupported fusion winner, or error). For the current 32B ACE-attention winners the metric comes from the prompt-only t=0 forward (`pre_detokenization=true`). Local CLI/TUI `/Users/msrk/Documents/furnace-guard/furnace` (`/Users/msrk/Documents/furnace-guard/furnace_cli.py`): `furnace score` / `wrap -- <agent>` / `tui -- <agent>`; `wrap` suppresses the command on non-ALLOW and pipes allowed prompts via stdin (`FURNACE_PROMPT` + `FURNACE_GUARD_JSON`). **Real Modal smoke PASSED** (modal 1.5.1) against the live Qwen2.5-32B nf4 profile — `score` returned BLOCK from `attention[last_minus_1_js] @ step 0`, `wrap` suppressed the wrapped output. **Caveat:** the threshold is derived from the ANLI calibration matrix — this is NOT yet a general harmful-prompt classifier; it needs domain calibration before BLOCK is trusted as a safety policy. Reference: `wiki/references/modal-cloud-extractor.md`.

**06-23 closures (provenance + precision-ladder §4 control).** (1) **72B byte-verify CLOSED** — the OOM guard is confirmed real (72B-bf16 raises ValueError on A100-80GB), so the existing Qwen2.5-72B 0.639/0.918 runs are **confirmed nf4** (the 2026-06-22 block's "inferred / not byte-verified" caveat is now resolved). (2) **Commit-equivalence intersection set CLOSED** — Qwen2.5-7B ANLI commit-dump across {nf4,int8,bf16,fp32}: per-rung YES/NO 93.5–97% (nf4 most decisive), pairwise commit agreement 85–90%, strict intersection 160/200 ⇒ cross-precision AUROC comparisons are **not** materially contaminated by answer-flips. A supplementary "commit-orbit" tangent (06-23/24) also ran — `commitment-convergence` (\~18.5% behavioral disagreement ceiling on ANLI, invariant to family/scale; scale erases format leakage), `orbital-prompt` ("Answer Anchor" `\n\nAnswer:` first-token collapser + 3-category leak taxonomy), `correctness-consensus` (**NEGATIVE** — consensus voting adds no value over the strongest single model on TriviaQA paired), and `dead-runs` (Falcon-180B OOM, Command A template-incompat) — **mostly supplementary / not-for-paper** per `wiki/state-of-play-2026-06-24.md`.

## Hot update for Claude (2026-06-22, torch cloud panel — Llama-3.3-70B FAMILY DISSOCIATION + 2nd ANLI orphan resolved; precision ladder wired)

A new exploratory **torch cloud panel** (Modal, `/Users/msrk/Documents/furnace-guard/modal_app.py`; reference `wiki/references/modal-cloud-extractor.md`) extends the scale axis onto GPU hardware the MLX seal can't run — **NON-byte-comparable** (torch + bitsandbytes 4-bit, reimplemented capture, stamped `backend="modal-torch"`; never pool with sealed/byte-comparable cells; **does NOT alter the sealed 18/20**). Faithfulness held by the o_proj reconstruction gate (cos≈1.0) + a pre-extract `validate` gate (YES/NO commit). Results (n=200/200, controls pass): **Qwen2.5-32B 2/2** (anli 0.790 / triviaqa 0.822 — **originally bf16, not nf4**: provenance bug caught 2026-06-23 by byte-identity check; the pre-patch runs were unstamped. A **true-nf4 32B** run was then done and *also* wins ACE attention, anli 0.763 / triviaqa 0.781) and **Qwen2.5-72B 2/2** (anli 0.639 / triviaqa 0.918, **confirmed nf4** — byte-verify CLOSED 2026-06-23: the OOM guard is real (72B-bf16 raises ValueError on A100-80GB)) — **all ACE attention penultimate-layer, winning solo** (72B shifts to inter-head JS sub-cells `js`/`js_no_bos`). Scale tier is **mixed precision**, not "all nf4." **Llama-3.3-70B 2/2 deployable (anli 0.703 / triviaqa 0.788) but BOTH win on RPV READOUT-volume at gen_step=1 (`neg_shadow_logvol_r1`, `fisher_eff_rank`), NOT ACE attention** → first scale cell where ACE doesn't win, consistent across both tasks ⇒ a **family dissociation in signal LOCUS**: Qwen family → attention-morphology (t=0 preparation), Llama family → readout-volume (gen_step=1 commit). Sharpens the "no universal *cell*; the universal thing is the *fitting procedure*, and even the universal *region* (attention-at-commit) is now dented" reading. Separately, **Llama-70B/anli 0.703 resolves the sealed `Llama-3.1-8B/anli` orphan as a scale/small-model artifact** — the **2nd** sealed ANLI orphan to close at scale (after gemma gen-3-12b 0.709 / gen-4 0.691), via an independent family ⇒ both sealed ANLI orphans confirmed scale artifacts. Pre-run **Codex adversarial pass on the 4-bit path = GREEN** (lm_head stays floating under `bnb_4bit`; one RMSNorm-eps flag resolved numerically inert). Detail: `wiki/results/llama-70b-scale-2026-06-22.md`; log tail 2026-06-22. **Paper:** the ACE-solo universality claim is already byte-comparable-cells-scoped (the gemma note); the Llama readout-locus win is a non-byte-comparable counterexample that *supports* keeping that scoping — folded as a scoped paragraph into `cc-draft.tex` §"scale and generation close the orphan" (2026-06-22). **Also wired (2026-06-22):** precision-ladder switch in `/Users/msrk/Documents/furnace-guard/modal_app.py` (`--precision {nf4|int8|bf16|fp32}`; **nf4 == historical 4-bit byte-for-byte**) + pre-registration `wiki/results/precision-ladder-prereg-2026-06-22.md` — a **confound-elimination** test ("is the rupture signal just quantization noise?"); H3 falsifier = ≥0.10 geom CI_lo drop nf4→bf16 on a deployable cell. Vehicles: Qwen2.5-7B full {nf4,int8,bf16,fp32} ladder + 32B {nf4,int8,bf16} confirm. **RAN 2026-06-22/23 → `wiki/results/precision-ladder-results-2026-06-22.md`:** H3 **falsified at the fixed-cell level** — robust signals are precision-invariant at 7B AND 32B (the signal is real computation, not quantization noise). Key method lesson: **cross-precision must be judged on FIXED CELLS, not the argmax winner** (the argmax + its OOB CI_lo are selection-noisy). Selection-instability AND int8/LLM.int8 degradation are **small-model artifacts** that wash out by 32B (32B winner stable, int8≈bf16). This run also **caught the 32B=bf16 provenance bug** and **de-confounded the family dissociation** (true-nf4 32B wins attention; Llama-nf4 wins readout — matched precision).

## Hot update for Claude (2026-06-21, gemma-4 generation-axis cell RESOLVED — orphan does NOT return at gen-4)

The pending generation axis (the headline blocker in the 2026-06-18 block below — now **DONE**, that "BANKED/Pending" note is **superseded**). `gemma-4-12B-it-qat-4bit` extracted via **mlx-vlm** in `.venv_gemma4` (no released mlx-lm implements `gemma4_unified`), fed to the same nested-OOB calibrator → **NON-byte-comparable** standalone gen-4 cell (mlx-vlm reimpl + version delta; never pool with sealed/byte-comparable cells). **Result: 2/2 deployable** — anli_r1 geom CI-lo **0.691** / primary 0.683; triviaqa_paired geom **0.751** / primary 0.748; controls pass; n=200/200; winner = Fusion `fusion_rank_mean_geom @ step 0` on both. **Generation axis does NOT reintroduce the sealed `gemma-3-4b/anli` orphan:** gen-4/anli 0.691 ≈ gen-3-12b/anli 0.709 (both PASS) vs gen-3-4b 0.403 (FAIL) ⇒ orphan confirmed a **scale / small-model gen-3 artifact**, not a generation-lineage property. **Bug found+fixed first (Bell-Burnell):** initial run was \~0.37 on BOTH tasks (full panel incl. confidence also \~0.369) because the io-plugin default `raw_passthrough` does not make gemma-4-it do the task (it continues the question text); under `apply_chat_template` it commits YES/NO p≈1.0 — fixed `stage_b/gemma4_full_extract.py` strat (diagnostic `stage_b/g4_diag.py`; no double-BOS). Wrinkle: both winners are Fusion, not the ACE-solo of the scale cells (paper footnote). Does NOT alter the sealed 18/20. Detail: `wiki/results/gemma-scale-extension-2026-06-18.md` (generation-axis section); log tail 2026-06-21. Caveats: non-byte-comparable; ACE recompute faithful (`G4Wrap` o_proj cos=1.0); readout half NOT independently parity-validated (D0/D1 off-by-one fixed; controls pass). Artifacts: `stage_b/profiles_ext/{anli_r1,triviaqa_paired}/gemma-4-12B-it_FIXED.matrix.npz`. **DONE 2026-06-21:** folded into the paper (`wiki/paper/cc-draft.tex` — §"scale and generation close the orphan"; daggered non-byte-comparable rows in `tab:ext`; "ACE-solo winner" claim scoped to byte-comparable cells; title/subtitle/abstract unchanged) AND milestone pushed to public `furnace-causalities` (commit bac2a82). Still open (user): Overleaf compile check of `cc-draft.tex` (no local LaTeX toolchain); optional regen of `fig5_scale_extension.pdf` to include gen-4.

## Hot update for Claude (2026-06-18, commit-confluence scale extension — gemma orphan resolved as scale)

Pre-registered, **byte-comparable** out-of-sample extension of the confluence seal (`commit-confluence/stage_b/PRE_REGISTRATION_EXT.md` + `run_ext.py`; profiles in `stage_b/profiles_ext/`; module hashes identical to seal, same seed 20260612 / fresh data / n=200 strict / nboot=2000; existing adapters, **no sealed-core edit**). Added `gemma-3-12b-it` (scale axis) + `Qwen2.5-14B-Instruct` (family control). **Result: 4/4 new cells deployable.** The sealed `gemma-3-4b/anli` orphan (geom CI_lo **0.403 FAIL**) → `gemma-3-12b/anli` **0.709 PASS** ⇒ that orphan is a **scale / small-model artifact** (gen-3); `Qwen2.5-14B/anli` **0.766 PASS** rules out a generic 12–14B failure. (g3-12b/trivia 0.929; Qwen-14b/trivia 0.597 marginal.) All winners ACE attention; confidence/fusion never sole winner; all 4 registered predictions confirmed. **Does NOT alter the sealed 18/20** (separate post-seal section). Detail: `wiki/results/gemma-scale-extension-2026-06-18.md`; log tail 2026-06-18.

**[SUPERSEDED 2026-06-21 — DONE; see the top hot-update block. gemma-4 is 2/2 deployable via the mlx-vlm reuse path; the upstream-mlx-lm wait was not required for the scale-orphan question.]** ~~Pending — the generation axis (`gemma-4-12B-it`) is BLOCKED on upstream support.~~ Investigated 2026-06-18 (parallel venv `commit-confluence/.venv_gemma4`, py3.12, mlx-lm 0.31.3 latest; model downloaded; see `stage_b/_ext_logs/gemma4_setup.log` + `introspect_gemma4.py`). The checkpoint is `model_type: gemma4_unified` (encoder-free multimodal); **no released mlx-lm (incl. 0.31.3) implements `gemma4_unified`** → `load()` raises "Model type gemma4_unified not supported." Its text stack (`gemma4_unified_text`) is a *novel* attention arch — dual head dims (sliding `head_dim=256` vs `global_head_dim=512`), per-layer-type KV heads (sliding `num_key_value_heads=8` vs `num_global_key_value_heads=1`), **partial-rotary (0.25) "proportional" RoPE on full-attn layers**, `attention_k_eq_v=true`, 5:1 sliding:full pattern, `final_logit_softcapping=30`, mixed 4/8-bit quant. The "load under plain `gemma4`" override is NOT viable (different ModelArgs). So gemma-4 needs EITHER (a) upstream mlx-lm to ship `gemma4_unified` (then a normal adapter), OR (b) a from-scratch port of the decoder + a faithful manual attention recompute for ACE (large, high-risk of silently-wrong signals). **UPDATE 2026-06-18:** `mlx-vlm` (parallel venv) DOES load `gemma4_unified` — `language_model` = 48-layer text decoder; attention `__call__` captured (`q_norm`/`k_norm`, `use_k_eq_v`, partial-RoPE, per-layer `n_kv_heads`/`head_dim`, fused `scaled_dot_product_attention`). So gemma-4 is **reachable**. But the t0 extraction passes (`collect_ace_matrix` via `attention_capture`, readout via `comprehensive_run.trace_pair_features`) load via mlx-lm and assume its attention layout, so a gemma-4 cell needs a **reimplementation**: an mlx-vlm loader shim + a gemma4-aware attention-weight recompute `softmax(q·kᵀ·scale+mask)` (post q/k-norm + partial-RoPE) + readout on the mlx-vlm model, feeding the *existing* calibrator, validated cross-impl on gemma-3-12b (where ground-truth signals exist). Est. \~a day, output **non-byte-comparable** (version + reimpl delta). Probe artifacts: `stage_b/try_loaders_gemma4.{py,sh}`, `_ext_logs/gemma4_loaders.log`. **Recommendation: prefer waiting for mlx-lm to add `gemma4_unified` (clean adapter, low risk); undertake the mlx-vlm reimplementation only if the user wants the generation-axis point now** — the scale-axis orphan question is already answered without it. **RESOLVED 2026-06-18 — BANKED (user chose to pause).** Built + proved the parallel-venv reuse path (extraction in `.venv_gemma4` via mlx-vlm ⟂ calibration in seal venv; the seal's stat code imports & runs under the venv). Wrote the readout extractor `stage_b/gemma4_readout_extract.py` + full spec `stage_b/GEMMA4_BUILD_SPEC.md`. A **Codex adversarial review + my code check found a confirmed off-by-one**: the seal's `gen_probs[0]` is the POST-commit step-1 dist `D1` (forward on `[prompt+gen_id]`), not the prompt-last `D0` the WIP used (surprise uses `D0`; RPV/null_ratio/p_max use `D1` — `pri_runtime.py:985-1118`). **Decision: wait for upstream mlx-lm to ship `gemma4_unified`, then reuse the seal's exact `trace_sample` verbatim** (genuinely comparable) rather than maintain a hand-port. All resumable; gemma-4 model + venv cached on disk (\~8GB). Next-step-if-resumed-sooner: mlx-lm transitive-parity harness on gemma-3-12b (fix `p_t`, validate readout vs seal matrix) BEFORE the ACE recompute. Also pending: whether to fold this extension into the paper and whether to push a public milestone.

## Hot update for Claude (2026-06-11, commit-confluence registered sealed verdict)

The **commit-confluence** dispatcher (a new repo at `/Users/msrk/Documents/commit-confluence`, public at `https://github.com/flowstyleliving/commit-confluence`, tag `prereg-seal-20260612`) unifies all in-house signals — ACE attention morphology + v3 null_ratio + RPV readout spectrum + surprise/p_max confidence + 2 fusion cells = a 29-cell panel — under the sealed nested-OOB selector, one CalibrationProfile per (model, task). It was hardened through **four** adversarial passes (Opus S-series, Opus C-series, Codex M-series, Codex v5) before launch.

**Registered verdict (fresh seed 20260612, 10 models × {ANLI, TriviaQA}, n=200, clean, controls passed):** geometric-only dispatcher **PASSES 18/20** (bar ≥17); strict full-panel PRIMARY **FAILS 18/20** (bar ≥19 → product claim falsified). Both endpoints fail the **same 2 ANLI cells** (`gemma-3-4b/anli` predicted, `Llama-3.1-8B/anli` new — the no-prior-ACE-seal model): confidence is **not** the backstop, coverage is 18/20 with or without it, the holes are genuine epistemic orphans. **No universal cell** — 12 distinct winners / 18 deployable. Full detail: `wiki/results/confluence-seal-2026-06-11.md`; log tail 2026-06-11.

Pending: (a) decide whether to publish the registered results from the tag (currently gitignored in the public snapshot); (b) run the pre-registered descriptive analyses E1 LOMO universality / E2 task-transfer / E3 label-efficiency (`stage_b/analyze_universality.py`, non-gating).

## 🗄️ HISTORICAL — SUPERSEDED 2026-07-25 (t0 re-sealed; see the 2026-07-25 block above) — Hot update for Claude (2026-06-07, T0 furnace → living morphology lab)

The user **relaxed the T0 seal**: `t0-morphology-furnace` is no longer a frozen archive — it is now the **living forward home for morphology research**. New, unsealed morphology candidates incubate in a top-level `exploratory/` area there (e.g. `exploratory/shadow-ambiguity/` for research-candidate #10), which is kept out of the sealed test suite (`pytest testpaths = tests` does not collect it). The sealed ACE/T0 core — repo-root modules, `tests/`, `experiments/t0-sealed/`, `paper/` — **stays frozen**; `exploratory/` is the only place live work lands.

This **supersedes** the 2026-06-06 instruction below ("do not mix later exploratory branches into the clean ACE/T0 archive unless the user explicitly asks"): the user has now explicitly asked — but only for the segregated `exploratory/` area; do not touch the sealed core.

Repo division going forward:
- **`t0-morphology-furnace`** = the morphology line — ACE (`W_u`-free attention morphology, sealed) + new `W_u`-using readout-morphology candidates (e.g. #10 shadow-ambiguity) in `exploratory/`.
- **`PRI_at_commitment`** = the PRI-detection line — v1/v2/v3, `pri_calibrator.py` / `pri_detector.py`, plus its own exploratory branches.

First inhabitant: `exploratory/shadow-ambiguity/test_shadow_ambiguity.py` — candidate #10's numpy identity/contract suite (7/7 green; dual-reviewed Claude + Codex, 2026-06-07). Context: `wiki/research-candidates.md` §10 and `wiki/log.md` 2026-06-07.

## Hot update for Claude (2026-06-06, T0 archive split)

Codex created the clean private ACE/T0 archive repo:

- GitHub: `https://github.com/flowstyleliving/t0-morphology-furnace`
- Local path: `/Users/msrk/Documents/t0-morphology-furnace`
- Branch: `main`
- Current archive commit after nomenclature cleanup: `7c7ccbb`
- Sealed tag: `t0-ace-sealed-2026-05-26`

What changed: the sealed ACE/t=0 morphology work was copied out of `PRI_at_commitment` into a fresh repo named **T0 Morphology Furnace**. The new repo intentionally contains only the canonical ACE/T0 archive: pre-registration, calibrator/detector/runtime core, T0 runner scripts, T0 tests, sealed 2026-05-26 profiles/data/logs, and paper figures. Exploratory residual-friction / projection-veto / attention-route / route-override branches and artifacts are excluded.

Codex then removed the old milestone label from the new repo's public-facing nomenclature. The archive now uses `T0` / `t0` naming throughout filenames, docs, runner scripts, experiment paths, paper paths, generated tex, logs, manifests, and tests. Verification: `rg` found zero old milestone-label strings in `/Users/msrk/Documents/t0-morphology-furnace`; `py_compile` passed; fast test slice passed (`87 passed, 2 deselected`).

Claude instruction: treat `t0-morphology-furnace` as the canonical ACE/T0 morphology repo going forward. `PRI_at_commitment` remains the working research repo with later exploratory branches; do not mix those later branches back into the clean ACE/T0 archive unless the user explicitly asks.

## Previous hot update for Claude (2026-06-06)

Latest Codex follow-up after the v5 same-`Δh` correction: candidate #9 spawned v6/v7/v8 exploratory branches in `PRI_at_commitment`. **Log this as the current correction-of-record after the v5 no-promote decision.**

**v6 — Projection Veto** (`codex/v6-projection-veto`): reintroduced `W_u` deliberately as a YES-vs-NO contrast bank to ask whether attention and MLP fight over the answer "steering wheel" rather than somewhere in residual space. Qwen2.5 ANLI R1 n=200: raw projection-veto over `null+route` clears strongly (`Δ=+0.110 [0.100,0.124]`), but the same-`Δh` projection baseline is identical (`Δ=+0.111`) and projection-budget also clears (`+0.105`). Net same-Δ projection-veto = **−0.0012**. Verdict: answer-axis conflict exists, but reads as projection-space budget / benign cancellation, **not clean Knowledge Veto**. Artifacts: `experiments/projection-veto/2026-06-06/run-02/`.

**v7 — Attention Route** (`codex/v7-attention-route`): tested a tempting route definition `p_a=<u_NO-YES,a>` from the v6 dump, no new model forward. Qwen2.5: signed attention-route over `null+route-size` is **negative/null** (`Δ=-0.0134 [-0.0333,-0.0022]`); absolute attention budget helps (`+0.0298`); MLP/final readout still strong (`+0.1209` / `+0.1185` beyond attention). Verdict: the route is **not** "attention write points YES/NO"; ACE's attention-channel morphology is the better route sensor. Artifacts: `experiments/attention-route/2026-06-06/run-01/`.

**v8 — ACE Route Override** (`codex/v8-ace-route-override`): used sealed v4/ACE `t=0` Qwen2.5 winner `attention::final_v_norm_lastq_weighted @ step 0` as the route sensor, rescored per prompt, then asked whether MLP/final/projection-veto add beyond ACE. Qwen2.5: ACE-route adds `+0.0623`; MLP/readout/override add beyond ACE (`+0.0579` to `+0.0704`). But projection-veto beyond ACE is raw `+0.0662`, same-Δ floor `+0.0635`, net **+0.0027**; after ACE+budget `+0.0025`, after ACE+same-Δ `+0.0007`. Shuffled-label ACE-route is warm (`+0.0354`), and sealed ACE profile provenance hashes drift from current code, so this is a drift-aware pilot, not a sealed reproduction. Verdict: **ACE route + MLP/readout is real/interesting; Knowledge Veto still net-null after same-Δ/budget. Do not promote.** Artifacts: `experiments/ace-route-override/2026-06-06/run-01/`.

Current synthesis for Claude: v5/v6/v8 all converge on the same lesson — late MLP/readout and ACE-route signals are real, but the "attention-vs-MLP veto" component collapses once identical-`Δh`, projection-budget, and shuffled/control floors are respected. Future work should treat ACE route as the durable t=0 route sensor and design a new statistic that beats same-Δ/budget before any sealed nested-OOB promotion.

Latest result to log/remember: candidate #9 / v5 residual-stream sub-layer friction **does not promote** as a clean Knowledge Veto under the new same-`Δh` benign cancellation baseline.

What Claude previously knew from the vault: same-day notes said "late-layer Qwen+Llama positive; pin late-layer window; promote Qwen cluster / maybe Llama." That is now superseded.

What Codex added in `PRI_at_commitment` on branch `codex/v5-residual-friction`:
- schema-v3 friction dumps with `Xbenign`, a same-`Δh` benign baseline: hold `Δh=a+m` fixed, match raw cancellation/norm budget, rotate disagreement off the consequential direction.
- `scripts/benign_cancellation_baseline.py`, `scripts/analyze_residual_budget.py`, and upgraded `scripts/analyze_friction_layer_profile.py`.
- `experiments/residual-friction/2026-06-06/run-07/` for Qwen2.5, Qwen3-8B, Llama3.2, Llama3.1.

Key numbers (ANLI R1 n=200, t=0): Qwen2.5 raw +0.1205 vs same-Δ floor +0.1129 → net +0.0076; Qwen3 raw +0.0955 vs floor +0.1235 → net −0.0280; Llama3.2 raw +0.0458 vs floor +0.0477 → net −0.0019; Llama3.1 raw +0.0150 vs floor +0.0171 → net −0.0021. Best selected 3-layer same-Δ residuals are small/post-hoc (≤ +0.0366).

Residual-budget diagnostic: friction adds essentially nothing after same-Δ benign (`friction|null+route+same-Δ`: Qwen2.5 +0.0134; Qwen3 −0.0036; Llama3.2 −0.0010; Llama3.1 −0.0043). Interpretation: the pilot mostly measured **benign cancellation / residual norm budgeting**, not directed epistemic veto. **Do not promote current v5 residual-friction to sealed nested-OOB.** See `wiki/results/residual-friction-pilot-2026-06-06.md` and `wiki/log.md` 2026-06-06 same-Δ correction.

## Project state — HISTORICAL DIGEST (frozen at 2026-05-15; pre-ACE / pre-T0)
> ⏳ **This block is a historical snapshot of the PRI v1–v3 era, frozen 2026-05-15.** It predates the v4/ACE seal (2026-05-26), the T0 morphology split (2026-06-06), the candidate-#9 no-promote correction (2026-06-06), and candidate #10 (2026-06-07). **For current state, read the "Hot update" blocks above + the tail of `wiki/log.md` first** — per the **Vault canon** HARD RULE below, any orientation block older than the latest log entry is historical unless explicitly marked "Hot update." Kept for provenance; do not quote as current.

Reverse-chronological digest. Detail in `wiki/log.md` + `wiki/milestones.md` + `wiki/results/`.

- **2026-05-15** — Two W_u-free diagnostics rolled up: `delta-sigma-onaxis` 7-model panel **[OPEN, leaning NEGATIVE]** (bivariate null·Δσ_n does not robustly beat null alone; Δσ_n *alone* wins on Phi-3.5 and Phi-4 with opposite signs); `inter-head-disagreement` JS-radius pivot on Mistral + Qwen 2.5 **[OPEN, mildly encouraging]** — first cross-architectural sign-stability ever (both sign `−` at final layer) but magnitude collapses Mistral 0.74 → Qwen 0.60. Needs Llama 3B + Qwen3 8B follow-up for full 4-model invariance test.
- **2026-05-13** — ANLI full sweep (11 models × R1/R2/R3 × n=50 = 33 profiles in 90 min) **RETIRES the meta-classifier definitively**. Fisher r=2 @ step 3 is noise (17+/15− sign split, median AUROC 0.551). Even within a single model, different ANLI rounds pick different winning cells/signs. 30/33 profiles fire deployability warnings at n=50 → per-(model, exact deployment distribution) calibration is the only honest framing. Same day: calibrator schema v1.1 ships — nested OOB bootstrap fixes Codex review's post-selection-bias finding (empirical demo: in-sample 0.911 → OOB 0.875, winner_stability 0.66 fires `winner_unstable`). First pytest suite (`tests/`, 50/50 green) lands.
- **2026-05-12** — `pri_calibrator.py` (commit 41d91e4) + `pri_detector.py` (commit 36ffbc7) ship to repo. Byte-exact reproducibility verified on self-test (reported AUROC = deployed AUROC, |Δ| < 1e-3). Detector adds pipeline-hash drift check + output-projection-kind verification + EOS-before-rupture handling. This is the **headline deliverable** — production library that retires the "label-free deployment" claim.
- **2026-05-11** — 5-tier IO plugins land (`pri_v2_io_plugins.py`): Tier 0.5 emphatic-closing parser + per-model `apply_chat_template` dispatch unblocks Mistral-Nemo / Gemma-3-1B / Dolphin behavioral gate.
- **2026-05-10** — **v3.2 amendment [FALSIFIED] at sealed plane.** All three pre-reg criteria fail: centered-Fisher does not rescue Qwen 3 (Δ=−0.05 vs ≥+0.10 threshold), regresses on Llama + Mistral by >0.1 AUROC, kl_discharged diverges in both directions. Adaptive-step v4-candidate #3 also **[DEGRADED]** — adaptive often *loses* to best-fixed-step by 0.30–0.49 AUROC. v3.1 sealed primaries unchanged (descriptive-only amendment scope). See `wiki/results/v3.2-results.md`.
- **2026-05-02** — Paper revision round 1 lands on `paper/pri-draft.tex` (reviewer-feedback PDF at `raw/feedback/`). 8-bullet feedback addressed; Sofroniew 2026 (Anthropic emotions) integrated 3×; LLMWiki/Karpathy bibitems removed. **2026-05-06**: Junjie Hu (HARP first author) replied warmly to outreach, flagged the Fisher-reweighting-vs-static-SVD partition as "meaningful extension." Substantive feedback still pending. Hu's likely probes tracked at `wiki/feedback/hu-anticipated-probes.md`.
- **2026-04-24/25** — **v3.1 fresh-seed replicate** (seed 20260423, Phase 1 + Phase 2). Sealed E18 PASSES 3/3 primaries at rank 1. RMSNorm `γ` Jacobian correction (J_n) lands in Fisher pullback — reshapes cross-model verdict shape but not the gate.
- **2026-04-23** — **v3 main run sealed E18 PASSES 3/3 at rank 1**: Llama 0.8593 [0.806, 0.908], Mistral 0.8638 [0.814, 0.910], Qwen 2.5 0.7274 [0.656, 0.795]. E19 `null_gated` interpretation gate **[FALSIFIED]** on all 4 tested models. Rank was not pinned in the sealed block; rank 1 chosen post-hoc as principled operating point (top-1 Fisher = commit direction).

## Validated, Falsified, Open

### Validated
- v2 family (non-diag Fisher) beats v1 on 3 primaries at the synthetic 2×2 benchmark (n=800/model). Diagonal FIM is unsafe (inverts on Llama/Mistral).
- v3 `null_ratio_post_rank1` sealed E18 PASSES 3/3 primaries at rank 1; replicated under fresh seed (v3.1).
- Production library (`pri_calibrator.py` + `pri_detector.py`) ships with byte-exact reproducibility self-test and OOB selection-bias-corrected CIs (schema v1.1).
- Per-(model, exact deployment distribution) calibration is the honest deployment framing — task-family or cross-round generalization does **not** currently transfer cleanly (ANLI R1 vs R2 vs R3 pick different cells/signs on the same model).

### Falsified / Retired
- **v3.2 centered-Fisher amendment** ([FALSIFIED] 2026-05-10, sealed plane).
- **Adaptive-step v4-cand #3** ([DEGRADED] 2026-05-10).
- **Meta-classifier v4-cand #4** ([RETIRED] 2026-05-13) — 33-profile ANLI sweep confirms family-based prediction is dead. The 2026-05-12 in-flight `handcrafted_v4` rule was also caught by Codex adversarial review for sign-flip (auroc_signed allowed sign-fitting on held-out labels) and in-sample fit (rule designed on all 11 oracle cells).
- **E19 `null_gated` interpretation gate** ([FALSIFIED] 2026-04-23 across 4 models).
- **"Label-free" deployment claim** (retired by calibrator ship).
- **Autoresearch loop** (retired 2026-04-14).
- **gpt-oss-20b**, **Gemma 3-1B** (excluded — too heavy / model-capability gate failure).

### Open
- **Inter-head JS-radius cross-arch invariance** (2026-05-15) — sign survives on Mistral + Qwen 2.5; needs Llama 3B + Qwen3 8B replicate for full invariance test. First W_u-free signal with cross-arch sign-stability we've ever seen.
- **Δσ_onaxis bivariate panel** (2026-05-15) — currently leaning negative; Phi-3.5 / Phi-4 sign-flip on Δσ_n *alone* worth a within-Phi-family follow-up.
- **Qwen 3 8B weak v3 signal** across 39-cell layer × rank landscape (commits at step 1 with surprise ≈ 0.96 while v2/v3 collapse). Qwen-family / scale-generation diagnostic.
- **v4-candidate #1** (empirical-variance gate parser) — operational `--gate-max-tokens 12` fix recovered Llama + Phi in v3.2 re-run; full empirical parser still leverage-rich for new families.
- **Small-n calibration** — 30/33 n=50 ANLI profiles fire deployability warnings. Need n ≥ 200 (or stronger priors) for clean deployable profiles outside the cleanest models.
- **Paper round-2 revision** — pending Hu substantive feedback; v4 future-work narrative in §5.4 references TriviaQA paired-prompt design (no numbers yet).

