# pri-v3/ — PRI v3 build docs (historical)

Grouped here on 2026-06-08 to declutter the wiki root. **v3 is sealed** (E18 PASSES 3/3 at rank 1, 2026-04-23; replicated v3.1). These are the build-time artifacts, kept for provenance — not live to-do lists.

- [[pri-v3-plan]] — the v3 plan + its **amendment audit trail** (sample-size bump, the rank-pinning lesson, preflight fixes, the J_n correction). Still actively cross-referenced (~22 inbound) as the canonical v3 provenance.
- [[v3-code-map]] — the "where to edit" code map written 2026-04-14. ⚠️ Line numbers have since drifted (repo grew calibrator/detector/ACE/v5–v8) — grep for function names, don't trust the line numbers.

For the v3 **verdict**, see [[results/v3-main-run]] + [[claims]] §2. For current state, read the [[log]] tail.
